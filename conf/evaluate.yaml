defaults:
  - _self_
  - encoder: full
  - override hydra/hydra_logging: disabled
  - override hydra/job_logging: disabled


env_params:
  fps: 10
  max_step: 100
  init_length: 4
  food_reward: 2.0
  living_bonus: 0.0
  death_penalty: -1.0
  width: 20
  height: 20
  block_size: 20

agent_path: models/agent.pkl
episodes: 10
seed: 42
render_mode: null

hydra:
  output_subdir: null
  run:
    dir: .
